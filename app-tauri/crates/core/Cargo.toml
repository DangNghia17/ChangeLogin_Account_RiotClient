[package]
name = "ram-core"
version = { workspace = true }
edition = { workspace = true }
rust-version = { workspace = true }
description = "Core logic for Riot Account Manager (storage, crypto, settings, Riot integration)"

[lib]
name = "ram_core"
path = "src/lib.rs"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
base64 = { workspace = true }
sha2 = { workspace = true }
aes = { workspace = true }
ecb = { workspace = true }
log = { workspace = true }

[target.'cfg(windows)'.dependencies]
enigo = "0.2"
windows = { version = "0.58", features = [
    "Win32_Foundation",
    "Win32_Security_Cryptography",
    "Win32_System_Memory",
] }
